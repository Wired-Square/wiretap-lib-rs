[package]
name = "wiretap-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Scalar-in, scalar-out codecs for the wire protocols WireTAP speaks: the CAN data length code table, GVRET, SLCAN, gs_usb, the SocketCAN frame layouts, the Test Pattern link validator and the ingest id-flag layout."
readme = "README.md"

# Empty, and it is meant to stay that way. README.md says why.
[dependencies]
